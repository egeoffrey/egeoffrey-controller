package: myhouse-controller
revision: 9
version: development
github: myhouse-project/myhouse-controller
dockerhub: myhouseproject/myhouse-controller
container_config:
  volumes:
    - ./data/config:/config
description: The myHouse controller manages the configuration of all the other modules, orchestrate sensors and run alerting rules
tags: controller
modules:
  - controller/logger
  - controller/config
  - controller/db
  - controller/alerter
  - controller/chatbot
  - controller/hub

